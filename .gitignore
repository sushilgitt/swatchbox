node_modules
.DS_Store

/.cache
/build
/app/build
/public/build/
/public/_dev
/app/public/build
/prisma/dev.sqlite
/prisma/dev.sqlite-journal
database.sqlite

.env
.env.*

# package-lock.json is committed so `npm ci` works in the Docker build
yarn.lock
pnpm-lock.yaml

/extensions/*/dist

# Ignore shopify files created during app dev
.shopify/*
.shopify.lock

# Coolify deployment credentials (contains API token + DB password)
/.coolify/

# Internal research/planning docs — keep out of the public repo
Variant-Swatch-King-Research.pdf
*.research.pdf

